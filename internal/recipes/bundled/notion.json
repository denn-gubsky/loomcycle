{
  "command": "npx",
  "args": ["-y", "@suekou/mcp-notion-server"],
  "env": {
    "NOTION_API_KEY": "${LOOMCYCLE_NOTION_API_KEY}"
  },

  "_loomcycle": {
    "description": "Notion API — page read/write, database queries, block-level edits.",
    "transport": "stdio",
    "pool_size": 2,
    "env_vars_required": ["LOOMCYCLE_NOTION_API_KEY"],
    "credentials": [],
    "schedule_compatible": true,
    "agent_prompt_hint": "Use this to read project notes, append summaries to pages, or query databases. Page IDs come from the Notion URL after the last dash."
  }
}
