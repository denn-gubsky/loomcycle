{
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-tavily"],
  "env": {
    "TAVILY_API_KEY": "${LOOMCYCLE_TAVILY_API_KEY}"
  },

  "_loomcycle": {
    "description": "Tavily AI search — fast LLM-shaped web search with citations.",
    "transport": "stdio",
    "pool_size": 2,
    "env_vars_required": ["LOOMCYCLE_TAVILY_API_KEY"],
    "credentials": [],
    "schedule_compatible": false,
    "agent_prompt_hint": "Use this for fresh web research with citations. Tavily returns LLM-friendly summaries — prefer over raw fetch when you need a quick survey of recent results."
  }
}
